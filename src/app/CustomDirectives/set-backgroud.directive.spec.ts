import { SetBackgroudDirective } from './set-backgroud.directive';

describe('SetBackgroudDirective', () => {
  it('should create an instance', () => {
    const directive = new SetBackgroudDirective();
    expect(directive).toBeTruthy();
  });
});
