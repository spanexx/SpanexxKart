import { SetHighlightsDirective } from './set-highlights.directive';

describe('SetHighlightsDirective', () => {
  it('should create an instance', () => {
    const directive = new SetHighlightsDirective();
    expect(directive).toBeTruthy();
  });
});
