import { DisableProductDirective } from './disable-product.directive';

describe('DisableProductDirective', () => {
  it('should create an instance', () => {
    const directive = new DisableProductDirective();
    expect(directive).toBeTruthy();
  });
});
